{
  "name": "electron",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:electron": "cross-env NODE_ENV=development electron ./electron/main.js",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:electron\"",
    
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "build:electron": "npm run build:backend && cross-env NODE_ENV=production electron-builder",
    "build": "npm run build:frontend && npm run build:backend && npm run build:electron"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.0"
  }
}
